{
  "name": "attrify",
  "version": "1.0.1",
  "description": "Attrify saves a user's initial-and-last, referrer and campaign (utm) query string parameters as cookies. Initial values are saved for ever and last are persisted for the length of the users browsing session. The cookies can then be passed to your marketing automation system, CRM, or email marketing system at any time during the users session for enhanced marketing attribution.",
  "homepage": "https://github.com/lukebussey/attrify",
  "repository": {
    "type": "git",
    "url": "git://github.com/lukebussey/attrify.git"
  },
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run build && npm run minify",
    "build": "browserify --standalone attrify index.js -o dist/attrify.js",
    "watch": "watchify --standalone attrify index.js -o dist/attrify.js",
    "minify": "uglifyjs dist/attrify.js -o dist/attrify.min.js",
    "test": "jshint index.js && mocha"
  },
  "authors": [
    "Luke Bussey <luke.bussey@gmail.com>"
  ],
  "keywords": [
    "campaign",
    "utm",
    "utm_campaign",
    "utm_source",
    "utm_medium",
    "utm_term",
    "utm_content"
  ],
  "license": "MIT",
  "dependencies": {
    "cookie": "^0.3.1",
    "deepmerge": "^1.3.0",
    "querystring": "^0.2.0"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "chai": "^3.4.1",
    "jsdom": "^7.2.2",
    "jshint": "^2.9.1",
    "mocha": "^2.3.4",
    "uglify-js": "^2.6.1",
    "watchify": "^3.7.0"
  }
}
